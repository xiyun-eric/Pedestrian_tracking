# ============================================================
# 动态场景多目标跟踪 - 依赖列表
# ============================================================
# 安装命令（在 dl_env 环境中执行）:
#   pip install -r requirements.txt
#
# GPU版PyTorch (CUDA 12.1) - 先执行:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# ============================================================

# 目标检测框架
ultralytics>=8.2.0          # YOLOv8/v11 检测模型

# 计算机视觉
opencv-python>=4.8.0        # 图像处理、视频读写、GUI
opencv-contrib-python>=4.8.0 # OpenCV扩展（含tracking模块）

# 科学计算
numpy>=1.24.0,<2.0.0
scipy>=1.10.0               # 匈牙利算法 linear_sum_assignment

# 机器学习
scikit-learn>=1.3.0         # 特征处理、评估指标

# 数据增强
albumentations>=1.3.0       # 图像增强

# 可视化
matplotlib>=3.7.0
pillow>=10.0.0

# 评估指标
motmetrics>=1.3.0           # MOT评估（MOTA, IDF1等）

# 工具
pyyaml>=6.0
tqdm>=4.65.0
rich>=13.0.0                # 美化终端输出

# ============================================================
# Qwen2-VL 大模型行人跟踪 - 依赖
# ============================================================
# HuggingFace 生态
transformers>=4.45.0        # Qwen2VLForConditionalGeneration
peft>=0.12.0                # LoRA / QLoRA 微调
accelerate>=0.34.0          # 分布式训练与模型加载
bitsandbytes>=0.43.0        # 4bit量化 (QLoRA)
qwen-vl-utils>=0.0.8       # Qwen2-VL 官方视觉处理工具
datasets>=3.0.0             # 数据集加载
