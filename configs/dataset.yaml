# 自采集行人跟踪数据集配置
# 用于 YO11 微调/LoRA微调
# 需先运行: python tools/prepare_data.py --input data/custom --output data/yolo_custom

# 数据集路径
path: D:/学习/大创/Pedestrian_tracking/data/yolo_custom

# 训练/验证集
train: images/train
val: images/val

# 类别定义
names:
  0: pedestrian  # 行人
  1: cyclist     # 骑车人
  2: car         # 车辆
  3: other       # 其他